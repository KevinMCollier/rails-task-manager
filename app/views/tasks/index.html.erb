<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <!-- if task is completed ✅ else 🟩 -->
      <%= task.completed? ? "✅" : "😱" %>
      <%= link_to task.title, task_path(task)  %>
      <%= link_to "🗑", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
    </li>
  <% end %>
</ul>

<p><%= link_to "Add a task", new_task_path %></p>
